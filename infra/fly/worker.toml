app = "voiceci-worker"
primary_region = "iad"

[build]
  dockerfile = "../../apps/worker/Dockerfile"

[env]
  NODE_ENV = "production"

# Worker has no HTTP service — it only processes jobs from Redis

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
